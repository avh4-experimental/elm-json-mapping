{
  "name": "elm-json-mapping",
  "scripts": {
    "test": "elm-test && npm run-script check",
    "check": "elm make --docs=documentation.json && elm-format --validate ."
  },
  "license": "MIT",
  "devDependencies": {
    "elm": "^0.19.0",
    "elm-format": "^0.8.1",
    "elm-test": "^0.19.0-rev6"
  }
}
